{
  "name": "kybera-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "CORS proxy for Kybera's LLM providers + OAuth token endpoints (Render Web Service)",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --watch index.ts"
  },
  "dependencies": {
    "hono": "^4.6.14"
  }
}
